GCD of a given TWO numbers. 
const prompt=require('prompt-sync')();
let a=Number(prompt("Enter a : "));
let b=Number(prompt("Enter b : "));
function gcd(a,b){
    while(b!=0)
        [a,b]=[b,a%b];
    return a;
}
let g=gcd(a,b);
console.log("GCD of "+a+","+b+" is "+g);
